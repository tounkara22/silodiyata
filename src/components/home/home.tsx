import { Fragment } from "react";
import styles from "./home.module.css";

const HomeScreen = (props: any) => {
  return (
    <div className={styles.hero}>
      <h3>Home page in progress</h3>
    </div>
  );
};

export default HomeScreen;
